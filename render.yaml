services:
  - type: web
    name: omniroute-render
    runtime: docker
    region: oregon
    autoDeploy: true
    envVars:
      - key: PORT
        value: "10000"
    disk:
      - name: omniroute-data
        mountPath: /home/site/.omniroute
        sizeGB: 1
